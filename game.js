const

const compare_words = async () => {
    // This function should return true or false if the user already found the word or if it is te case, telling if the word does not exist (at least not in wour dictionary) and an array stating each letter with its respective color
};

const get_input_word = async () => {
    // This will request a word to te user, if the word does not eexist this will not count to the total of attempts in the game
};

const choose_word = async () => {
    // This will take a random word from a list of common english or spanis words
};

const new_game = async () => {
    // Here is the general logic of the game
};

module.exports = {
  new_game,
};
